import React, { useState } from "react";
import { Card, Typography } from "@mui/material";

const _ContractCard = () => {
  return (
    <Card> 계약서 카드
    </Card>
  );
};

export default _ContractCard;
